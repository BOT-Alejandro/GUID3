<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Dark) -->
    <style name="Theme.GUID3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores y configuraciones del tema oscuro -->
        <item name="colorPrimary">@color/AzulPrincipal</item>
        <item name="colorPrimaryVariant">@color/AzulSecundario</item>
        <item name="colorOnPrimary">@color/Blanco</item>

        <!-- Fondo y texto del modo oscuro -->
        <item name="android:windowBackground">@color/FondoOscuro</item>
        <item name="android:textColorPrimary">@color/TextoOscuro</item>
        <item name="android:textColorSecondary">@color/Blanco</item>

        <!-- Colores de botones -->
        <item name="colorSecondary">@color/Blanco</item>
        <item name="colorSecondaryVariant">@color/GrisOscuro</item>
        <item name="colorOnSecondary">@color/Negro</item>

        <!-- Colores de botones FAB -->
        <item name="colorTertiary">@color/GrisOscuro</item>
        <item name="colorOnTertiary">@color/GrisOscuro</item>


        <!-- Barra de estado -->
        <item name="android:statusBarColor">@color/GrisOscuro</item>

        <!-- Colores de iconos -->
        <item name= "android:iconTint">@color/Blanco</item>

    </style>
</resources>
